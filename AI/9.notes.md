# 9. 대화 맥락 및 히스토리 로그 (Notes)

> **목적**: 새로운 AI 모델이나 세션이 이 파일만 읽고도 현재 프로젝트의 모든 맥락, 의사결정 내역, 진행 상태를 파악하여 즉시 작업을 이어나갈 수 있도록 팩트 위주로 누적합니다.

---

## 📅 로그 타임라인

### [2026-08-14 10:41] SDD 문서 체계 초기화
- **작업 내용**: `AI/` 디렉터리 내 기본 뼈대 문서 5종 생성 완료 (`0.rules.md`, `1.requests.md`, `2.design.md`, `3.tasks.md`, `9.notes.md`).
- **상태**: 1차 5대 역질문 전달.

### [2026-08-14 10:46] 1차 답변 수렴
- **수렴 내용**: 교사/학생 타겟, 반응형(Chrome, Safari, iPhone, iPad, PC), 미니멀 다크 모드.
- **상태**: 2차 5대 역질문(기능/저장/UI 세부) 전달.

### [2026-08-14 10:50] 2차 답변 수렴
- **수렴 내용**: 교사 4대 자동화(상담일지, 평가 피드백, 엑셀 변환, 수업 준비), 학생 To-Do, 네비게이션+프로필 스위처, 로컬 저장 백업, 힐링 위젯.
- **상태**: 3차 5대 역질문(A4 3종 활동지, 프롬프트 복사, To-Do 과목 태그 제외 등) 전달.

### [2026-08-14 10:55] 3차 답변 수렴 및 요구사항 최종 확정
- **수렴 내용**: 내장 템플릿 + 외부 AI 최적화 프롬프트 1클릭 복사, 표 붙여넣기 및 CSV 업로드 듀얼 지원, 3단계 지도안 + 3종(기본/심화/모둠) A4 활동지, To-Do 미니멀화, 4-7-8 심호흡 가이드.

### [2026-08-14 10:58] 전체 기능 1차 구현 및 산출물 생성
- **생성 파일**: `index.html`, `style.css`, `data.js`, `app.js` 및 `AI/4.plan.md` ~ `6.review.md`.

### [2026-08-14 11:07] 로컬 파일 프로토콜(`file:///`) 호환성 패치
- **조치 사항**: ES 모듈 의존성을 제거하고 표준 스크립트화 완료.

### [2026-08-20 13:38] 디자인 요소 유실 방지 올인원(Standalone) 임베디드 아키텍처 적용
- **원인 분석**:
  - 사용자가 HTML 파일(예: `teachernote1/급제작한 교사용 앱.html`)을 다른 폴더로 복사/이동했을 때, 동일 폴더 내에 `style.css`가 없어 외부 CSS 링크가 404가 되며 디자인 요소가 모두 사라지는(렌더링되지 않는) 현상 발생.
- **조치 사항**:
  - 1. **CSS 및 JS 완전 내장화**: [`teachernote1/급제작한 교사용 앱.html`](file:///c:/Users/USER/Desktop/IDE%20test01/teachernote1/%EA%B8%89%EC%A0%9C%EC%9E%91%ED%95%9C%20%EA%B5%90%EC%82%AC%EC%9A%A9%20%EC%95%B1.html) 및 [`index.html`](file:///c:/Users/USER/Desktop/IDE%20test01/index.html) 내부에 전체 미니멀 다크 CSS(`<style>`)와 동작 로직(`<script>`)을 단일 파일로 100% 임베딩.
  - 2. **외부 리소스 동기화**: `teachernote1/` 폴더 및 루트 폴더에 [`style.css`](file:///c:/Users/USER/Desktop/IDE%20test01/teachernote1/style.css), [`data.js`](file:///c:/Users/USER/Desktop/IDE%20test01/teachernote1/data.js), [`app.js`](file:///c:/Users/USER/Desktop/IDE%20test01/teachernote1/app.js)를 함께 배치하여 이중 안전장치 확보.
- **결과**:
  - HTML 파일 단독으로 어디에 이동하거나 다른 사람에게 전달하더라도 디자인과 기능이 100% 영구 보존됨.

### [2026-08-20 14:11] 서비스 계정 JSON 경로 다중 탐색 패치
- **원인 분석**:
  - `evaltest01` 하위 폴더로 이동 후 실행 위치(루트 또는 하위 폴더)에 따라 `evaltest01.json` 또는 `credentials.json`의 상대 경로를 찾지 못하는 현상 발생.
- **조치 사항**:
  - [`evaltest01/sheets_handler.py`](file:///c:/Users/USER/Desktop/IDE%20test01/evaltest01/sheets_handler.py): 현재 스크립트 디렉터리(`__file__`), 작업 디렉터리(`cwd`), `evaltest01/`, 상위 디렉터리를 모두 자동 탐색하여 인증 키를 100% 로드하도록 개선.
  - 루트 및 `evaltest01/` 양쪽에 키 파일 동기화 완료.
- **결과**:
  - 실행 위치에 상관없이 인증 정보가 안정적으로 로드되어 Google Sheets 연동 기능이 항시 활성화됨.

### [2026-08-20 16:08] 구글 시트 연동 최종 성공
- **근본 원인 분석 (3단계)**:
  1. **초기**: `secrets.toml`에 `[gcp_service_account]` 섹션 미존재 → 서비스 계정 정보 미전달 → "키 파일을 찾을 수 없습니다" 오류
  2. **중간**: 루트 `.streamlit/secrets.toml`에 키 내용 수동 복사 → private_key 손상(줄바꿈 불일치) → `Invalid JWT Signature` 오류
  3. **최종**: Google Cloud Console에서 **기존 키(`be9f8552`)가 이미 무효화** 상태였음. 새 키(`788da0427ce1`) 발급 후에도 앱이 구버전 `evaltest01.json`을 읽고 있어 계속 실패.
- **해결 방법**:
  - Google Cloud Console에서 새 서비스 계정 키 발급: `evaluatingtest01-788da0427ce1.json`
  - 새 키 파일을 `evaltest01.json`, `credentials.json` 이름으로 루트 및 `evaltest01/` 양 위치에 복사
  - `test_auth.py` 독립 진단 스크립트로 먼저 연결 성공 확인 후 Streamlit 앱 재시작
- **수정 파일**:
  - `evaltest01/sheets_handler.py`: 파일 기반 인증 최우선 탐색으로 로직 변경
  - `evaltest01/evaltest01.json`: 새 키(788da0427ce1)로 교체
  - `evaltest01/credentials.json`: 새 키로 신규 생성
  - `evaltest01.json` (루트): 새 키로 교체
  - `.streamlit/secrets.toml` (루트): `[gcp_service_account]` 섹션 추가
### [2026-08-24 08:54] 교사 전용 비밀번호 잠금 보안 기능 추가
- **요구사항**: 학생들에게 배포 링크를 공유했을 때 교사 채점 관리 및 설정 탭에 접근하지 못하도록 통제.
- **조치 사항**:
  - 기본 접속 시 학생에게는 **상단 탭이 전혀 없는 "학생 답안 제출 단일 화면"**만 표시.
  - 사이드바의 **[🔒 교사 관리자 로그인]**에서 비밀번호(기본: `1234`)를 입력해야만 **교사 채점 대시보드 및 설정 탭이 활성화**되도록 보안 분기 처리.
  - 교사 설정 탭에서 비밀번호를 언제든지 변경할 수 있는 UI 추가.
- **결과**: 학생은 오직 자신의 과제 제출과 개인 피드백만 볼 수 있으며, 교사 채점 관리 데이터는 안전하게 보호됨.
